# Baked into the production bundle by `vite build`.
# These are PUBLIC SPA identifiers (they ship inside the JS bundle by design)
# — safe to commit. Committing them guarantees any machine can produce a
# working production build; without them the Auth0 domain bakes as
# `undefined` and login redirects to https://undefined/authorize.
VITE_AUTH0_DOMAIN=dev-n0833ddaix42sr23.us.auth0.com
VITE_AUTH0_CLIENT_ID=LA11pKQ6PFceQOm3dzB9M5iFkbLUFrVB
VITE_AUTH0_AUDIENCE=https://popcore/api
